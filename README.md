# xor-transfer-learning-pruning
Transfer learning on logic gates using a neural network trained for XOR with pruning techniques to improve efficiency and reuse knowledge for OR and AND gate prediction.
